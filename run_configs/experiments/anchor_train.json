{
  "_purpose": "RL-anchor trainings for the section-7.4 ablation: standard architectures (ppo_transformer, rainbow_dqn) on the exact HC-v1 world and reward (baseline.json), fixed 4-tech fleet. 300 episodes at 200k sim each; eval every 25 episodes (3 deterministic), checkpoint every 50 with save_best.",
  "mode": "evaluated_training",
  "seed": 42,
  "n_episodes": 300,
  "log_interval": 10,
  "eval": {
    "enabled": true,
    "interval": 25,
    "n_episodes": 3,
    "deterministic": true
  },
  "checkpoint": {
    "enabled": true,
    "interval": 50,
    "dir": "checkpoints/anchors",
    "save_best": true
  },
  "wandb": {
    "enabled": true,
    "project": "kata-set-transformer",
    "log_interval": 1,
    "tags": ["anchor", "rl-ablation"]
  },
  "reports": {
    "enabled": true,
    "dir": "reports",
    "log_steps": false
  }
}
