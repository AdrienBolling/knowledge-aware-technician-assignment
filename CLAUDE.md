# CLAUDE.md — Knowledge-Aware Technician Assignment (FactoReal / KATA)

Research paper: **"Knowledge-Aware Technician Allocation: The Long-Term Impact of Technician Upskilling"**
(Bolling, Kubler, Ruiz Rodriguez, Le Traon — SnT). Target: *Journal of Manufacturing Systems*, elsarticle two-column.
Thesis: RL dispatching with human-centric reward (fleet knowledge growth + fatigue balance) beats production-only reward over long horizons.

## Paths (PROTECTED)

| What | Where |
|---|---|
| Manuscript (git **submodule**) | `paper/Manuscript.tex` (+ `paper/biblio.bib`, `paper/figures/`) |
| Simulator (code name `kata`) | `src/kata/` — `env.py` (Gym env), `core/config.py`, `metrics.py` |
| Experiments | `src/experiment/runner.py`, configs in `run_configs/benchmark_suite/` (7 configs) |
| Framework paper the survey builds on | `Human-resource-allocation-problem-in-the-Industry-4-0-A-reference-framework.pdf` (Grillo et al. 2022; bib key `grilloHumanResourceAllocation2022`) |
| Taxonomy-table audit trail | `analysis/taxonomy_row_verification.md` (per-paper reasons for every 2026-07-06 change) |
| Build | `cd paper && latexmk -pdf -interaction=nonstopmode Manuscript.tex` (2 passes to settle refs) |

## Manuscript status (2026-07-06)

| Section | State |
|---|---|
| §1 Intro, §2 Related Work (+ Table 1 taxonomy), §4 FactoReal, §5 MDP Implementation, §7 Results (7.1 setup, 7.2 scales, 7.3 objectives ablation; 6 tables + 2 figures from 2026-07-07 benchmarks), Appendix A | drafted prose |
| §3 HRAP-4.0 formalization | **notation table only — body is commented out/empty** |
| §6 Method (hybrid tokens, PLE/T2V/Fourier, Transformer, PPO), §7.4 RL ablation, §8 Discussion, §9 Conclusion | skeleton `\emph{}` placeholders |
| Abstract | red placeholder — benchmark numbers now available (see `reports/hvp_eval/`, memory note benchmark-results-hvp) |

## Key decisions

| Decision | Rationale |
|---|---|
| Table 1 common block (2026-07-15, strict-Grillo rework): objectives `PROF COST EXE RAT PERM QUAL` (classic, Grillo order) / `BAL WELL` (4.0) / `TARDY AVAIL FLEX RISK MOD` (ours); constraints `TPP PPT RES` (classic) / `DISR REC` (4.0); Scope + Method "ours" | User: use strictly Grillo's classic categories; legend descriptions are shortened Grillo quotes |
| `PROF`/`PERM` columns kept **empty** (0 marks); `ELIG` dropped; `TPP`/`PPT` marked for the 32 works with an explicit assignment decision (11 model/predict-only rows unmarked — list in audit trail) | Empty columns = a finding (drift from classic objectives); ELIG duplicated Grillo's qualified variants inside the cardinality families; Grillo §3.2: TPP/PPT/RES "mandatory" for an HRAP model. Grand total 385→434, This-work row 17→19 |
| Row corrections applied conservatively (43 adds, 11 removals; felberbauer B→A, goncalves C→A) | Only abstract-verifiable changes; judgment calls flagged in `analysis/taxonomy_row_verification.md` |
| Table 1 / §2 restructure (2026-07-20): Scope block → **Context**, trimmed to the 3 descriptors the method uses (`HET STO HIST`); Industrial-dynamics + Broader-literature blocks **merged** into one `B. Broader literature` (17 rows); This-work row `D`→`C`; §2 split into 2.1 Common Parameters + 2.2 Human Factors, with a corpus-selection paragraph (43 works, 2 criteria = blocks A/B) | User request; grand total 434→405; `\resizebox` 0.88\textwidth to keep the float one page |
| §5 documents the MDP **implementation** (state schema, event loop, action semantics, reward table `tab:reward_components`); §6 Method covers the learned encoders | §5 written from actual code (`env.py::_build_token_stream/_set_obs/_action_mask/_reward_for_assignment`) — keep in sync if obs schema or reward stack changes |
| §5 framing: event-driven POMDP with semi-MDP remark; γ discounts decision steps, not sim time | Matches implementation; soften if reviewers balk |
| Reward regimes (production-only vs human-centric subsets/coefficients) deferred to §7 Setup | Env capability vs experiment configuration split |
| **MLM pretraining DROPPED from the paper** (2026-07-21): keywords, contribution bullet, §7.4 reserved text (now representation + RL-anchor ablations), §8 deployment-pattern clause, threats, conclusion contributions all scrubbed | User decision — never run, cut scope; §7.4 focuses on hybrid-token vs bucket-token + PPOTransformer/Rainbow anchors |
| **v4 world = both R5 mechanisms ON everywhere** (2026-07-28): knowledge decay (interval 5000) + Kijima via new global `sim.repair.default_restoration_alpha=0.25` (`RepairConfig`; per-component α wins) in all 9 benchmark_suite configs — training AND eval | User decision after discovering both were default-off through the whole `hvp_eval_v3` generation; decay kills the reviewer's "saturation artifact" critique (5M knowledge 1.4M→~75k maintained). v3 gen archived untouched at `reports/hvp_eval_v3`; v4 gen at `reports/hvp_eval_v4` |
| **Semi-MDP γ^Δt discounting default for new runs** (2026-07-30): `time_based_discount=true`, γ=0.9999 PER SIM-T.U. (~10k t.u. horizon); PBRS shaping made time-consistent (`F = γₚ^Δt·Φ(s′)−Φ(s)`, γₚ=0.9999) | Per-decision γ made the agent more myopic in time units exactly when the factory was busiest; γ=1 rejected (critic variance, variable-horizon returns). dt recorded from `info["sim_time"]` (vec loop now forwards it); λ stays per-decision. `tests/test_gae.py` 10 tests |
| **v5 reward package** in `run_configs/benchmark_suite/train_multiscale_v5.json` (= v4 recipe + reward changes ONLY): PBRS knowledge credit, `workload_balance` 0.5, `fleet_availability` 0.5 | Floored `knowledge_increment` pays for knowledge CHURN under decay (drop unpenalised, regain re-paid); balance term was never on despite being in the thesis; availability densely prices the measured 5M illness blowup. Launcher `potential_knowledge_reward=null` now respects env config |
| **Benchmark best AND last checkpoints** (user, 2026-07-24): both canonicalised + evaluated everywhere | Inline eval (5 eps/50 rounds) is a noisy selector — proven right: last>best at baseline, best>last at industrial (ranking flips with scale) |
| **Traditional MLP baselines = set-flatten architecture ablation** (2026-08-13): a2c_mlp/grpo_mlp/dql_mlp consume the SAME set obs as HTT, flattened (`SetObsFlattener`: vocab-driven one-hots + stateless symlog, out_dim 5204) into plain 512×512 MLPs, padded masked Discrete(30) head | "Same information, traditional network" — kills the architecture×observation-richness confound; fleet-independent vocab + fixed head = every scenario (unlike token anchors). Scratch/no-BC (DQN can't BC-init), per-decision γ 0.997 (no semi-MDP/PopArt — those are contribution stack). Trained on `train_multiscale_v5_mlp.json` (RANGE fleets U(4,30) — per-slot heads need every action index; ratio mode reaches slots 27-29 with p<2%) |
| **GRPO-MLP: group = 8 complete episodes on one scenario at FIXED 275k horizon** (train_hydra pins both) | Episode-sum outcome z-scored in-group must not encode exogenous draws — horizon spread gave corr(horizon, return) ≈ −0.7 (empirically confirmed); old `grpo.py` NOT reused (ratio≡1 by construction) |
| **ft reward fine-tunes: one lever per variant, same seed 4242** (protect = availability 2.0 + balance 1.0; fatigue = fatigue_cost 2.5; quality = repair_quality 2.5), 100 eps @ lr 3e-5 cosine from hc_v6 final.pt | Attribution stays clean (same world sequence, only reward differs); components are running-normalized so coefficients are comparable shares |

## Blockers / Warnings

- **Eval StepMetric snapshot bug (found + fixed 2026-08-14, ultracode audit)**: `run_episode` copied the TERMINAL step's `info["metrics"]` verbatim, so `repair_quality`/`mttr_rolling`/`repair_time_delta(_per)` in every episodes.csv ever generated were single last-decision snapshots (one assignment; one 50-repair window of ~540k). Fixed (episode-mean accumulation, `_episode_kpis`, tests/test_eval_aggregation.py); `scripts/repair_stepmetric_columns.py` rebuilt v6w from steps.csv.gz (backups `episodes.csv.presnapshotfix`; `repair_time_delta` unrecoverable — not in step records). **Rank tables were never affected** (KPI_SPEC uses episode metrics only). RETIRED NARRATIVES: "learned-agent quality collapse/anomaly" (true spread ±0.01–0.03 = parity, e.g. lifecycle hc_v6 0.319 vs ft_quality 0.344, corr(quality,mttr)=−0.875 as mechanics demand) and all "final rolling MTTR" end-state claims (terminal-window lottery; greedy's 149.4 was its 97th-percentile draw). Older trees (hvp_eval_v3/v4) still carry snapshot semantics — repair before quoting quality from them. Audit also verified: reward chain clean (positive affine normalization — but env.py:1854/config docstrings claim scale-only division while `reward_normalizer.py:84` subtracts the mean), `busy_technician` component dead under masking (near-zero std hazard), reward-normalizer state never checkpointed.
- **Scenario-rotation off-by-one (found + fixed 2026-08-13)**: `KataEnv.__init__`'s bootstrap build consumed one sampler draw, so every `episodes_per_scenario` block in ALL historical trainings straddled two factories (7+1 instead of 8-8). Fixed in BOTH training env paths (`runner._build_env` + `vec_env._build_worker_env` call `reset_scenario_cache()` post-construction); the benchmark harness was deliberately NOT touched so v6w eval sequences stay byte-identical. Consequence: future training worlds differ by one draw from historical ones.
- **Analyzer silently drops unlabeled agents**: `analyze_hvp_results.py` filters episodes to `AGENT_ORDER` — any eval key missing from `AGENT_LABELS` vanishes from every table/ranking while sitting in episodes.csv (bit hc_v6 on 2026-08-13; hc_v5 pair was also missing — both fixed). Add the label in the same commit as any new eval key.
- `zhangGraphNeuralNetworks2022` **citation mismatch**: bib entry is a GNN-book chapter, row is coded as crowdsourcing assignment paper — needs the intended reference.
- ~~Placeholder cites~~ RESOLVED 2026-07-07: `ref`→bouajaja2017survey, `Marcelo,ref2`→ruizrodriguez2022a+joo+calzavara, `MarceloESWA`→ruiz-rodriguez_dynamic_2024, `Chica2017`→chica2017simheuristics (new entry). **Author check**: intended self-cites for `Marcelo/ref2`? (`bollingKnowledgeawareLearningfocusedMultiObjective` exists in bib but lacks year/venue.)
- ~~Legacy ECAI tail~~ RESOLVED 2026-07-07: `refcheck.sty` defines `\comment`, so `\begin{comment}` blocks were silently RENDERING (6pp of legacy in every PDF + the 5 build errors). Both legacy blocks **deleted** (recoverable from git, commit 50c180d). Build now 0 errors / 0 undefined / 0 multiply-defined, 27pp.
- `refcheck` package prints label names in margins — drop before submission (also the \comment definition source).
- ~~Masked-heuristics re-run~~ DONE 2026-07-15 (all 4 scenarios) and §6 fully refreshed. Prior headline: HC-RL is the only agent to beat informed heuristics (industrial rank 1.83). Co-author merge dfb4f08 integrated (HTT-RL stays contribution #2).
- **HEADLINE state (2026-07-30; supersedes the 2026-07-20 threat — those numbers are retired, different seed+world)**: informed(†)/reward-oracle(‡) baselines still top raw industrial throughput, but hc_v3 is best-learned and (v4 world) hc_v3_last passes SPT†/ReserveSpec†; the honest-information framing + human-centric profile (knowledge #1, illness fewest) is the defensible headline. **Open problem: 5M** — all learned agents fall below classic heuristics (illness blowup; objective, not distribution — FT refuted horizon exposure). v5 (illness pricing + PBRS + γ^Δt) is the designed answer; §6/§7/abstract claims stay frozen until v4/v5 numbers land.
- **GAE off-by-one FOUND + FIXED (2026-07-20 pm)** — `ppo_set_transformer.py::_compute_gae` consumed `dones[t+1]` instead of `dones[t]`: cross-episode advantage/value-target leakage whenever a rollout buffer held interior episode boundaries (vec training = HC-v2 only), and terminal credit severed at n−2 in **all** trainings — with `terminal_finished_products`/`terminal_fleet_knowledge` ENABLED in `baseline.json`, the terminal bonuses were effectively inert beyond the final action for HC-v1/PO too. Fixed + `tests/test_gae.py` (5 tests). The 100k headline threat may be partly this bug's artifact. **Retrain running** since 2026-07-20 12:53Z on serval: `checkpoints/hc_v2_gaefix` = GAE fix + PopArt, no GRU, γ0.997/λ0.97, vec6, HC-v1's exact reward (`baseline.json`) — a clean infra ablation; log `reports/train_hc_v2_gaefix.log` (marker `DONE_TRAIN_GAEFIX`), wandb `tfvgwmpo`, ETA ~3–4 d.
- **HC-v2 is NOT a valid negative ablation** (2026-07-20 pm review): its regression (industrial quality 0.32 vs HC-v1 0.69) is confounded by (a) the GAE bug fully expressed in vec training, (b) a reward change (`baseline_crit.json` adds criticality+downtime), (c) zero-BPTT GRU + an inline-eval RNN-state clobber (`runner.py::_inline_eval` advances training worker 0's hidden state — still unfixed, moot for non-GRU runs). Do **not** write it into §7.4 as evidence against the improvements; keep HC-v1 as the paper agent meanwhile.
- **§7 prose contradicts its own tables** (2026-07-20 review; fixes deferred at user request): (1) l.1784 "best value of *every* directional KPI" — HC is 6th/7 on workload balance (its own rank 1.83 proves it); (2) l.~1936 "best MTTR of the entire benchmark (61.6)" — LF 56.8 / SQ 59.7 in the same table; (3) l.~1938 "highest availability of all agents in every window" — RR 0.852 > HC 0.848. Should-fix list: baseline-MTTR exception to the "all scales" claim, "advantage grows" (it shrinks 34%→23%), abstract "fatigue-related absences" (metric counts all disruptions) and "several multiples" (it's 1.8×), LF/SQ 0.01 MTTR overclaim, "time-averaged" captions (per-decision means), `IllTechnicianCount` docstring (sums events, not techs). Everything else — all 8 tables + ~40 inline numbers — verified exact vs the CSVs.
- Notation audit **mostly applied 2026-07-07**: Acronyms block rebuilt (15 real acronyms), `t^c_base`→`c_time(τ)`, `c^⋆`→`c^*`, `m_F`+wrong formula→`m_f` (lin/exp), `m_k(k_i,τ)`→`m_k(τ,i)`, `φ:𝒯→[0,S]²` collision removed (φ prose + grid shape G), disruption-cell comma typo, ρ_i added (lr renamed — "diminishing-returns", lower ρ = faster learner). **Still open**: λ_c (reward coeff) vs λ_i (Jaber), β triple-use (Weibull/disruptions), `s` saturation scale vs state in §5 reward caption, `Q_t` vs `\mathcal{Q}(t)`.
- Full-text double-checks pending on Table 1: ali TEMP, alvarez SIM/ML, caricato MKS/CMP/TEMP, suer MKS, mcdonald FORG, staruch CAP; henao is a retail case study kept in block A.

## Next steps

0. **State-of-play artifacts (2026-08-14, corrected data)**: full-roster standings https://claude.ai/code/artifact/a2159780-a8b0-497c-8f12-4e2c4d2294bc ; ft levers https://claude.ai/code/artifact/a76daa9d-2a7f-4966-a3bf-8b287c65da35 . Corrected overall (22-agent roster, NOT comparable to 16-agent reads): TOPSIS-emp 6.03 > greedy‡ 6.97 > TOPSIS† 7.43 > hc_v6 8.37 > ft_protect_last 8.67. ft verdicts: quality lever = #1 industrial products 2,228 (> greedy‡) + best learned MTTR; fatigue lever = industrial illness −9% + #1 lifecycle knowledge 25.2k; protect lever = holds products, doesn't fix illness (symptom≠cause). A dgy chain (background) waits on `V6 FT QUEUE DONE` → git pull → repair → re-analyze → pulls very_long local.
1. **Queue state (2026-08-14 ~23:00Z restructure, user authorized any GPU)** — check markers on any session start: (a) **lifecycle benchmark DONE + read** (`V6W LIFECYCLE DONE` 2026-08-13 20:15Z; results in item 2b); (b) **ft reward fine-tunes** (zellij `v6ft`, log `reports/v6_ft_queue.log`, marker `V6 FT QUEUE DONE`): protect+fatigue trained rc=0, quality finished ~23:00Z → 24-part benchmark stage on GPU 1 (~10 h); (c) **traditional baselines RELAUNCHED 22:47Z gate-skipped** (commit 823c645: `TRAD_SKIP_GATE=1`, lanes a2c→GPU0 vec5, grpo+dql→GPU2; benchmark matrix now includes **lifecycle**; zellij `trad`, marker `TRAD BASELINES DONE`); (d) **local 4080 runs the ft lifecycle parts** (`scripts/local_lifecycle_parts.sh`, log `reports/local_lifecycle_parts.log`, parts in local `reports/hvp_v6w_parts/ft_*/lifecycle` — ship to dgy's parts tree + re-merge lifecycle when done; ft_quality pair auto-follows via a local watcher chain). On completion: read analyses, then the §6/§7/abstract number work — §7.4 gets the "same information, traditional architecture" MLP-anchor story. ⚠ v3/v4/gaefix/anchor/human checkpoints still ONLY on serval-paris (down, re-verified 2026-08-13) — evaluate into the same idempotent parts tree on its return.
2. **Lifecycle read (2026-08-14)**: hc_v6 3rd/16 (5.67, best honest-info; greedy‡ 3.5, SPT† 5.33); hc_v6_last products 30,663 ≈ greedy‡ 30,674 (−0.04%) with #1 knowledge 25.0k despite 5 highest-knowledge retirements; **final rolling MTTR: hc_v6_last 89.8 (improving, ep-mean 119.7) vs greedy‡ 149.4 (degrading, ep-mean 114.8)** — cleanest thesis contrast yet; learned repair quality anomalously low 0.10–0.13 (flag with MTTR when writing §7); exact-tie triples (batch_milp/Hungarian†/SPT†; least_fatigued/shortest_queue) — n=1, less independent info than 16 rows suggest. 5-scenario overall: emp-topsis 4.63 > greedy‡ 5.27 > TOPSIS† 5.57 > hc_v6 6.03.
2. **v6w standard generation DONE + read (2026-08-07/13)**: `reports/hvp_eval_v6w` local. **hc_v6 4th overall of 16 (mean rank 6.13)** — best-learned placement ever; **5M collapse reversed**: hc_v6 #1 raw 5M products (110,295, above greedy_reward‡ 110,211), hc_v6/last #1-2 final knowledge (65.2k/65.5k, above every baseline); industrial 3rd (2164, above all honest baselines); baseline tied 3rd. Weak spots: small_scale (noise regime), repair quality 0.26-0.39, illness/availability (fleet protection — the ft-queue target). Only honest baseline still ahead overall: TOPSIS-emp (4.29). Wall-time (`scripts/compare_wall_time.py`, artifacts in the root): heuristics ~2.6-3.0 ms/decision (≈ env floor), greedy_reward‡ 18.3, hc_v6 28.2 (~11× floor; 7.9 h/5M-episode vs 47 min for TOPSIS†).
2. **Serval is DOWN (2026-08-04; still down 2026-08-13)** — host dark at 10.6.36.22 (VPN verified fine: gateway answers, /24 swept, host-key matched nothing; needs power-cycle/IT). v5 queue status unknown; it was 2/600 eps at 2026-08-03 ~09:30Z. **On serval return, FIRST: `ls checkpoints/hc_v5/set_transformer_round*.pt | wc -l`** — if > 200, defect D1 (below) means the run tail + any extension trained at lr=0 and the plateau verdict is unsupported (v5-vs-v4 stays internally fair — same bug both sides; read v5 as a lower bound). Then resume the normal queue-monitor flow (log `reports/v5_train_queue.log`, marker `DONE_TRAIN_V5`, wandb `xb7yx45l`).
2. **Agent-improvement report delivered** (2026-08-04, user-requested; no code changed): `analysis/agent_improvement_report.md`. Nine verified defects (six major): **D1** cosine-LR hits exactly 0 mid-run (`train_hydra.py:90-101` 60-t.u./dec heuristic vs ~24 measured → ~40% of v4/v5 at lr=0; extensions resume inside the dead schedule → plateau gate self-masking); **D2** dropout 0.1 never disabled (no `net.eval()` anywhere — biases PPO ratios AND every benchmark number); **D3** within-slot role aliasing (`_SlotFuser` zeroes role embeddings + mean-pool → MATCH/FATIGUE/ETA values permutation-indistinguishable per slot); **D4** 5M feature corruption (count-PLE clamps at 10k vs ~75k knowledge; raw-time Time2Vec; Fourier `input_scale` unpersisted, eval-rebuilt 18× off — magnitude bounded by FT/gaefix counter-evidence); **D5** checkpoint selector noise (per-round seeds, unseeded horizon draw, drifting eval reward normalizer); **D6** GAE off-by-one still live in `ppo_transformer.py` (anchors' §7.4 gap partly artifact). Roadmap P0–P7 in the report; `copy.deepcopy(env)` clone for lookahead **empirically impossible** (SimPy generators). 114 citations adversarially verified (102 clean, 0 fabricated).
2. **Fix §6/§7 false claims + abstract** with the final best agent's numbers (user-deferred); wire `table_footnote.tex` under result tables. §7.4 anchors now exist (trained in queue: `checkpoints/anchors/`, old-world pair in `anchors_oldworld`).
3. Notation fix package (λ_c vs λ_i, β triple-use, `s` scale vs state, Q_t vs 𝒬(t)); write §3 formalization body; `zhangGraphNeuralNetworks2022`→`zhangReinforcementLearningMethod2022` swap (verified, awaiting sign-off).
4. **Push both repos** (large local commit pile since 2026-07-21 push — awaiting user's word).

## Session notes (2026-08-13 — v6w read, lifecycle+ft+trad queues, MLP baseline package)

- **v6w had finished cleanly 2026-08-07** but the analyzer's missing hc_v6 labels silently dropped both learned agents from every table (data was fine) — fixed, 81 MB merged tree pulled local, analysis regenerated. Results in Next steps item 2.
- **Artifacts**: KPI-vs-sim-time curves (5 agents × 6 KPIs × 4 scenarios) https://claude.ai/code/artifact/f2d0534b-c5ea-4ce0-b72e-f3902d600c06 — 5M signature: hc_v6 rolling MTTR FALLS (86→82) while greedy‡ degrades, knowledge keeps growing while TOPSIS† flatlines = the upskilling payoff made visible; fatigue rides 0.19 vs 0.15 (protection gap is a constant offset). Training stability https://claude.ai/code/artifact/8e74ca9d-98f6-4187-8c94-e90226a5f88d — infra-stable/signal-plateaued: entropy never collapsed (dip 0.11→0.38), KL early-stops on 199/447 updates (44% — LR/epochs at trust-region edge), vf_loss 0.35→0.003, PopArt σ 45→655→~555, LR floor reached; returns trendless after ~ep150, final-50 strongest (+3,760 vs −3,590 first-50).
- **User asks executed**: lifecycle benchmark launched (16 agents, was never run — infra existed since 2026-08-04); ft reward fine-tune queue built+launched (levers in Key decisions; eval keys `ft_*`/`ft_*_last` + labels wired); `scripts/compare_wall_time.py` (ms/decision + s/1M t.u., pooled ratio-of-sums, tex+csv artifacts).
- **Traditional-baselines package (ultracode, commit c8a758f)**: 4 workflows (map → 4-implementer build → 4-lens review with 18 Fable verifications → 4-fixer round), ~4.2M subagent tokens. 17 confirmed findings fixed — headliners: scenario-rotation off-by-one (Blockers), GRPO horizon-sum confound (corr −0.71, empirically reproduced), untrained slots 27-29 under ratio fleets (→ range-mode world), DQL exploration/replay drew from the simulator's global RNG stream (→ private streams, persisted in ckpt). New: `src/agents/networks/mlp_encoder.py`, `a2c/a2c_mlp.py`, `grpo/grpo_mlp.py`, `dqn/dql_mlp.py`, 5 test files, per-family injection branch in train_hydra, eval MLP branch (`n_actions=max_techs`, `legacy_obs=False` always), `SET_OBS_AGENT_TYPES` single-sourced in `experiment/config.py`. Suite 644. Micro-train + micro-eval smokes proved every family end-to-end BEFORE review.
- **Ops**: this shell has no ssh-agent — dgy pulls need `eval $(ssh-agent -s) && ssh-add ~/.ssh/id_ed25519 && ssh -A ...; ssh-agent -k`. Commits fb72b9b→c8a758f all pushed; dgy at c8a758f, 131 package tests green there. In-session watchers (byte-offset marker polls) die with the session — the queue logs/markers in Next steps item 1 are the durable interface.

## Session notes (2026-08-06 — v6 DONE, curve analysis, v6w generation launched)

- **v6 vec training DONE** (600 eps / 19 h 15 m / rc=0 on dgy; 447 updates; LR verified live-annealing 3.0e-4→1.5e-5 floor — first vec run ever with a working schedule). Canonicalise picked `final.pt` as last (VEC-1/3 paying off); `best.pt` = ep-401 eval peak (+256 ± 766).
- **Training read (curve artifact: https://claude.ai/code/artifact/8ff8c06b-b217-4015-b4ac-427838f0e988): plateau from ~ep 400.** Eval products 304→929→925 while illness 865→1083 and availability 0.659→0.628 — throughput learned, fleet protection NOT (**reward-design signal** for the deferred work); knowledge ~27.9k / quality 0.30 / balance 0.97 flat; entropy 0.26→0.38 (no collapse); quarter medians all negative, rolling mean trendless after ~ep 150; final-50 eps = strongest stretch → final.pt not stale. Returns are v5-reward/corrected-world — NOT comparable to v3–v5 curves.
- **BC corrected-world 88.2%** (old-world 88.4%) — the 74.1→88 jump was the v6 architecture, not world luck.
- **hc_v5 checkpoints do not exist** (training died at 2/600 when serval-paris went dark). Decide on serval return: rerun v5 under fixed infra or drop the v5 point (v6 recipe supersedes it).
- **Ops**: per-episode vec returns live ONLY in the offline wandb file — extract via `wandb.sdk.internal.datastore` scan of history records, key = `item.key or "/".join(item.nested_key)`. zellij: `action write-chars` into a background session is unreliable — `zellij --session S run -- cmd` works (used for both the relaunch and the benchmark queue). uni.lu VPN is per-login-session (NM profile; interactive secret: `nmcli connection up "uni.lu VPN" --ask`).

## Session notes (2026-08-05 — vec re-enabled, KATA-1 world fix, v6 vec relaunch)

- **User reversals/decisions**: parallel envs are BACK (user: "fix parallel envs locally" — the earlier "SimPy ill-suited" verdict traced to the *lookahead deepcopy* failure, not AsyncVectorEnv, which never pickles live envs); **zellij over tmux for all future job launches** (named sessions; installed 0.44.3 to dgy ~/.local/bin); KATA-1 fix approved — travel time "either" (→ let 15 activate), **failure-wise knowledge REQUIRED live from now on**; v6 serial run CUT at ~ep 200 and relaunched as vec5 on dgy (GPU 0, ≤6 cores).
- **Vec audit** (ultracode: 4 Opus lenses + Fable synthesis, ~850k tok): 17 verified defects. Headliners: vec loop never saved `final.pt` (**every v3/v4/v5 "last" = last periodic round, ~11% early at v5 settings**); D1 `total_updates` sizing was vec-cadence-only → **the dgy serial v6 trained at near-constant LR** (600 updates vs schedule sized 3356; warmup covered eps 1–134); vec inline-eval stats computed then DISCARDED (no eval/* for v3–v5); vec autoreset episodes 2+ drew disruption timing from OS entropy; serial loop never seeded process-global RNGs (horizon draws unpinned); eval/ckpt intervals meant rounds in vec but episodes in serial; vocab drift between generations RE-INDEXES keys (not append-only) → `_resize_token_embedding` prefix-copy would silently scramble rows.
- **Vec fix package (commit 255560a, suite 509)**: final ckpt in vec loop; cadence-aware `total_updates` (serial = episodes, warmup ep/25); eval logging in vec; derived-seed autoresets (SeededResetWrapper; sanity script now fingerprints episode 2 — bit-identical solo-vs-vec); `_run_episode` seeds globals (SER-1); episode-denominated intervals both modes (VEC-7 — old round-keyed values on vec configs now fire ~2× more evals); vocab guard at load AND vec-build (`_resolve_agent_vocab`) + non-prefix resize aborts; autoreset_mode pinned NEXT_STEP + guard; mp context pinned fork; sync-mode n>1 warns; queue scripts canonicalise final>round*>ep*>best. `tests/test_vec_training.py` (6). Fable adversarial review: SHIP — the 2 riskiest fixes re-simulated pre-fix and shown to genuinely fail.
- **KATA-1 (commit 7d9f547, suite 516) — WORLD CHANGE at generation boundary**: `GymTechnician`/`GymTechDispatcher` captured `get_config()` at import while launchers point KATA_CONF_PATH at a nonexistent file → **effective travel_time was 10 (configs/paper say 15) and failure_wise_knowledge_parameters False (configs say true) in ALL trainings and evals ever**. Fix = required `sim_cfg` DI (ScenarioBuilder + dispatcher.sim_cfg for lifecycle hires), both singleton captures deleted, no default on purpose. Survey: no other inert sim.* surface (2 latent always-overridden fallbacks: scenario.py:185, env.py:404). `tests/test_config_injection.py` (7, incl. per-failure-key multipliers + lifecycle-hire path + meta-guard). Consequences: existing hvp_eval_v4 CSVs stay internally valid (all agents 10/False both sides); **next benchmark generation = whole roster re-evaluated under corrected world; BC re-collected** (done in relaunch); paper appendix Δ_travel=15 becomes TRUE automatically.
- **v6 vec relaunch on dgy** (`scripts/dgy_v6_train.sh` updated): corrected world + both packages, parallel_envs=5 (5 workers + updater ≈ 6 cores), GPU 0, fresh BC (bc_topsis_v6 re-collect — old one archived, world changed), eval_interval=200 EPISODES, seed 42, zellij session. Serial partial (~200 eps, ep*.pt ckpts) archived as `checkpoints/hc_v6_serial_partial` + log renamed — read it as "no LR annealing, best from ≤1 eval" if ever used. Local pre-flight: 20-min typical run (vec5, real horizons) gated before cutover.
- **Interpretation caveats now attached to v6**: v6-vs-v5 spans fix package + corrected world + vec training — attribution further coarsened (user accepted); serial-partial is NOT a clean ablation (LR schedule bug).
- Monitor hygiene: dgy watcher re-armed as new-content-only (byte offsets; the 2026-08-04 stale ABORT line can't re-fire).

## Session notes (2026-08-04 eve — review fixes, PUSH, v6 LAUNCHED on dgy-serval)

- **User decisions**: lifecycle env = eval-only (later); **no env parallelisation from now on** (SimPy deemed ill-suited → single-env training, classic serial loop); **push authorized and done** (origin/main a5c64d2→dc28af1→c8a2275+; the July embargo pile is now on GitHub).
- **Adversarial review of the fix package** (4 lenses → per-finding verification, 24 confirmed/1 refuted) → second fix round, suite **503**: LR re-arm now uses the CONSTRUCTOR lr (`_ctor_lr`) — scheduler load restores the checkpoint's base_lrs which would silently override a halved fine-tune LR (empirically reproduced); **`legacy_obs_quirks` env flag** — pre-v6 checkpoints (detected by missing D3 keys in `improvements`) are evaluated under the pre-fix D11/D12 encodings they trained on (never-trained `KEY=T/F` embedding rows would put them OOD); token anchors always legacy; retired techs' disruption loops terminate (no phantom illness); machine retirement closes downtime/processing intervals + drains the input buffer (feeder blocked-put starvation) + last-of-type selector guard + feeder tolerates machine-less types; `replace_machine` template now optional = like-for-like (lifecycle.json switched to it); lifecycle scheduler refuses factory-less resets. **merge_hvp_parts was DESTRUCTIVE** (overwrote dest with only the parts) — now non-destructive + `--scenario` arg; ⚠ if the v5 queue on serval-paris reached its merge, `hvp_eval_v4/very_long` was clobbered to hc_v5-only rows — **on serval-paris return: re-merge from `reports/hvp_v4_vl_parts` + `hvp_v5_vl_parts`**. analyze_hvp_results knows `lifecycle`; heuristics sized to max_techs under lifecycle.
- **v6 TRAINING LAUNCHED on dgy-serval** (dgyserval01.uni.lux, `serval-dgy` alias): repo at `~/repositories/knowledge-aware-technician-assignment` (agent-forwarded git via ssh.github.com:443 — port 22 firewalled; dgy has no GitHub key of its own), uv-managed venv python 3.13. **DGX quirks**: GPU3 dead ("Unknown Error") — use GPUs 0-2; **lockfile torch (2.11+cu130) has NO sm_70 kernels → venv carries a deliberate torch 2.7.1+cu126 downgrade; every uv run there MUST be `--no-sync`** (a bare `uv run` reinstalls the incompatible torch); `~/.local/bin` not on tmux non-login PATH (cost one instant-abort launch, rc=127). Launch: tmux session `v6train`, GPU 0, `scripts/dgy_v6_train.sh` (BC re-collect with v6 arch → 600 eps single-env → canonicalise; no benchmarks). 21 smoke tests green on dgy; dry-run compose OK. Wall-clock estimate: ~2-4 days (single-env; eval_interval=200 to keep the 5-ep inline evals from burning a day).
- Suggestions report: `analysis/improvement_suggestions.md` (companion to the full audit report).

## Session notes (2026-08-04 pm — D-fix package, lifecycle events, v6 prepared)

- **User triage of the defect report**: fix D1–D6 (D2 "fix in eval"; D5 = keep best+last, no selector work; D4 = knowledge saturation is FINE — years-scale sim — instead build a "lifecycle" eval), then report suggestions. Reward design still deferred.
- **Fix package implemented (suite 501 pass, +21 tests)**: **D1** `_cosine_warmup_lr` min_factor floor (default 0.05, `lr_min_factor` ctor param), `tu_per_decision: 24` in conf/train.yaml sizes `total_updates` (~335 for v5-style runs, was 200), `_rearm_lr_schedule_if_exhausted()` on load (extensions no longer dead) — all via new `PPOAgentInfraMixin` shared by BOTH agent classes (SetTransformerAgent does NOT subclass PPOTransformerAgent — the duplication trap that stranded the GAE fix). **D2** `_eval_mode_if(deterministic)` around action-selection forwards + `net.eval()` after load in the benchmark harness + dropout 0.0 in the v6 agent JSON. **D3** `_SlotFuser` `role_binding` (keep role embeddings + nonlinear per-position binder — additive alone cancels in the mean) + `use_feature_context` (per-feature-across-techs context, the user's two-view idea); flags default OFF (old ckpts identical), persisted in `improvements`, honoured by the eval loader; `tests/test_set_encoder_d3.py` pins the aliasing defect itself. **D6** parent GAE `dones[t]` fix + parent-class tests.
- **TWO NEW OBS BUGS found while mapping lifecycle (D11/D12), both fixed**: **D11** set-mode boolean tokens emitted `KEY=TRUE/FALSE` but the frozen vocab registers `KEY=T/F` → **all six boolean tokens (BUSY/DISRUPT/BROKEN/PROC/IS_CURRENT/HAS_T) were `<UNK>` in every set-mode observation — no trained agent ever saw busy/broken/disruption state** (fix in `_SetEmitter.bool`; flat stream keeps TRUE/FALSE — first fix broke flat mode, caught by suite). **D12** `_machine_id_from_machine` read `.id` (never set; `machine_id` is the attr) → CPython `id()` fallback → set-obs BD_COUNT/DOWNTIME always 0, MEAN_TBF ≡ sim_time. Both mean the current benchmark numbers under-serve learned agents; re-eval of old ckpts after D11 is a train/eval-mismatch question (they LEARNED with UNK bools — flag when interpreting).
- **Lifecycle events implemented** (user's D4 replacement): `GymEnvConfig.lifecycle_events` (`LifecycleEventConfig`: time/kind/count/template/select), SimPy scheduler process fires exactly at sim time; add/retire technicians (tombstone: keeps slot for index alignment, masked everywhere, excluded from knowledge mean, step() rejects, structured-obs busy=1), add/retire/replace machines (`Machine.retired` stop-flag; `FactoryHandles` retained on dispatcher — router/feeders/type-queues were previously discarded locals; feeder lists are ALIASED with machines_by_type — guarded); machine retirement defers while broken (verified live: event at 1.2M fired at 1.366M after repair). `tests/test_lifecycle.py` (7 tests, real baseline world).
- **Configs prepared**: `run_configs/agents/set_transformer_v6.json` (+conf/agent symlink; dropout 0, binder+feature-context on), `run_configs/benchmark_suite/lifecycle.json` (industrial 5M, 24 techs/70-80 machines for cap headroom, 10-event schedule: senior retirements→trainee/junior hires, capacity adds, most-breakdowns renewals), eval-script `lifecycle` scenario + `hc_v6` keys, `scripts/serval_v6_train_queue.sh` (BC re-collect with v6 arch — D3 invalidates bc_topsis_v4 — then exact v5 recipe/world/reward, then std benchmarks + full-roster lifecycle fan-out). Hydra dry-run compose OK.
- **v6 bundle caveat (deliberate, user-directed)**: v6 = infra fixes + D3 architecture + D11/D12 obs fixes in ONE hop vs v5 — attribution to individual components is sacrificed; the honest comparison is v6-vs-v5 as "fix package"; micro-ablations remain possible later from the flags.
- Improvement report updated: `analysis/agent_improvement_report.md`.

## Session notes (2026-08-03 — v4 verdict, sync, v5 launch)

- **v4 train chain DONE** (2026-07-30 14:33Z, all rc=0): 600 eps in ~5.2 h, plateau confirmed first try (last-quarter return −0.37 vs −0.01, no extensions), best+last canonicalised, all 4 scenarios merged into `hvp_eval_v4`, analysis clean.
- **DECISIVE: training under decay DOES teach knowledge maintenance.** 5M final fleet knowledge: hc_v4_last 76.4k / hc_v4 76.1k = top-tier (batch_milp/Hungarian† 76.3k, SPT† 75.6k, greedy‡ 75.1k; gaefix 77.1k #1) vs v3 family 56.6–59.5k. Mechanism: v3 family peaks HIGHEST (~80–81k) then decays away — builds but never maintains; hc_v4 peaks 79k and keeps it.
- **Illness blowup fixed at 5M**: hc_v4 ~50.0k events ≈ best heuristics (TOPSIS† 49.9k) vs v3's 81k. Remaining gap = **5M throughput**: hc_v4 107.8k vs greedy‡ 124.2k / TOPSIS† 122.4k / SPT† 120.8k, also below gaefix 117.6k + human 115.3k — exactly what v5 targets. 5M is n=1 (noise caveat); best-learned at 5M by mean rank is gaefix (9.8) > hc_v4_last (11.8).
- **Industrial**: hc_v3_last stays best learned (2268, mean rank 8.2); hc_v4 2210 / hc_v4_last 2171. Overall mean rank: TOPSIS† 7.5 > empirical_topsis 7.9 (best honest) > greedy‡ 8.5 … hc_v3_last 11.3 best learned. Quality quirk: at 5M hc_v4 best-ckpt quality 0.32 vs hc_v4_last 0.70 — best/last split keeps paying.
- **Embargo lifted + synced** (tar-over-ssh; serval has NO rsync): local e8e8300…532d1dc code state on serval, checksum-verified incl. `conf/env` symlinks. New commit 532d1dc = `scripts/serval_v5_train_queue.sh` + hc_v5/hc_v5_last eval keys (`checkpoints/hc_v5_final/`).
- **v5 LAUNCHED** (armed 2026-08-03 08:46Z, pid 3829639, wandb `xb7yx45l`): BC-init `bc_topsis_v4` (reused — same world), exact v4 recipe, only variable = objective (PBRS knowledge credit + workload_balance 0.5 + fleet_availability 0.5 + γ^Δt 0.9999). Pre-launch on serval: 13 tests green (test_gae + test_hydra_configs), all 4 queue entry points `--help` OK under PYTHONPATH, dry-run compose verified (γ 0.9999 semi-MDP, λ 0.98, PopArt, potential-based knowledge). GPU was fully free at launch.
- Ops: launching `nohup ... &` through ssh holds the channel open (child inherits the pipe) — background the ssh locally or add `</dev/null`; the remote queue survives killing the local ssh.

## Session notes (2026-07-27→30 — v3/v4 benchmark generations, FT verdict, v4 world, v5 package)

- **v3 generation complete** (`reports/hvp_eval_v3`, fresh seed 20260722, old 2026-07-20 numbers RETIRED): industrial — greedy‡ 2651 > TOPSIS† 2605 > SPT† 2602 > Hungarian† 2582 > TOPSIS-emp 2572 > **hc_v3 2561** (best learned; #1 fleet knowledge 87k, fewest illness 957, −3.4% vs reward oracle). **5M: thesis NOT supported** — heuristics 139–147k vs hc_v3 132k, illness 73k vs ~46k, fatigue σ 0.30+. Baseline: gaefix 3068 > HC-v1 3046 (gaefix collapses at industrial → scale-specific). Anchors: PPO-TRF rank 12/22, Rainbow 20/22, cannot transfer scales (= §7.4 story).
- **Long-horizon fine-tune REFUTED the horizon-exposure hypothesis**: hc_v3_last + 60 eps at U(1.5M,5M), matched T2V scale, LR halved → 5M flat (132.0k vs 132.4k), illness unchanged; baseline improved to 3050 (best learned there). Conclusion: the 5M collapse is an OBJECTIVE problem (γ myopia + unpriced illness), not distribution shift. Mid≈final ⇒ converged.
- **v4-world generation complete** (`reports/hvp_eval_v4`, all agents re-evaluated + anchors retrained; old anchors → `checkpoints/anchors_oldworld`): mechanisms cost everyone ~11% industrial throughput; **hc_v3_last now #6 industrial (2268), ahead of SPT† (2126) and ReserveSpec†** — harsher world favours the knowledge-aware policy. Knowledge saturation broken (5M knowledge 1.4M→~75k maintained). v3 family's 5M knowledge (57–59k) now BELOW top heuristics (70–76k) — its style fails to maintain under decay.
- **v4 training** (from-scratch, fresh v4-world BC 74%-class, exact v3 recipe, world = only variable): first attempt CUDA-OOM'd at update 1 (launched concurrently with the bench chain — anchor training 7.8 GB + 14 eval procs saturated the card; no retry → 1 idle day). Relaunched 2026-07-30 07:15Z; 204/600 at last check. Queue auto-runs plateau gate → canonicalise → benchmark merge.
- **Ops lessons**: serval venv has NO editable install — every queue entry point needs `PYTHONPATH=$PWD/src` (first queue run burned 5 days failing instantly on `ModuleNotFoundError: kata` while the user was away; only train_hydra self-bootstraps). Smoke-test EVERY entry point on serval itself. Don't co-schedule GPU-hungry stages (or retry OOM). Parallel fan-out (per-agent out-roots + `merge_hvp_parts.py`) collapsed the 2.5-day sequential ladder to ~6 h; parts are idempotent (skip if episodes.csv exists).
- **v5 prepared + implemented** (commits 51d4921/24bce60/f3c3358): semi-MDP γ^Δt, v5 reward package, time-consistent PBRS (see Key decisions). Validated: dry-run, live probe (384/600 negative knowledge increments under random policy = un-floored maintenance pressure), suite 487. `conf/env` symlinks were gitignored by the `ENV/` pattern since creation — now tracked.
- **Sync embargo**: do NOT sync local commits to serval until the v4 chain (incl. possible extensions) finishes — `conf/train.yaml` now defaults to γ^Δt and extensions relaunch `train_hydra` with current defaults, which would silently change the objective mid-experiment.

## Session notes (2026-07-22 — v3 stall root-caused: horizon freeze)

- **The v3 "stalls" were never a hang — episodes could not terminate.** Root cause (`env.py::_is_done` + advance-loop guard at l.750): the advance loop refuses to execute events beyond `_episode_max_sim_time`, freezing `now` just *below* the horizon, while `_is_done` required `_sim_time() >= horizon`. Under dt-polling this was masked (dt-aligned events land exactly on integer horizons); the event-driven engine samples continuous times → exact hit has probability 0 → every episode ran to `max_episode_steps` (20k) via no-op steps that each still paid a full policy forward (~50–250 ms). Both serval v3 stalls (16 CPU-h "computing") and the local repro (0 eps in 26 min) were this purgatory. ac32e81's event-storm floor was correct but NOT the stall cause.
- **Fix** (commit e8e8300): `_is_done` also terminates when every remaining event lies beyond the horizon AND no decision is pending (`current_request is None` and queue empty — the queue clause matters: the done-check runs before the queue-pop in `_advance_until_next_ticket`, and a ticket created by the last in-horizon event must still be decided). 3 regression tests in `tests/test_env.py` (non-integer horizon). Suite **480 pass**. Repro: 8-ep multiscale run 0-in-26-min → **8/8 in 3:08, RC=0**; solo probe terminates at sim 200000.0 in 8,249 decisions.
- Debug forensics that got there: faulthandler SIGUSR1 dumps (3 snapshots at 3 different lines → progressing, not hung), isolated forward timing (36–48 ms uncontended vs 244 ms contended — the earlier "244 ms forward" was measurement contention with the running repro), env probe (~1 ms/step, ~22 t.u./decision, no aging blowup to 137k), worker CPU ~3% (mostly blocked on main). The `python -c` child with 0.03 s CPU is the multiprocessing resource tracker, not a dead worker.
- **v3 RELAUNCHED 2026-07-22 11:54 local** on serval via `train_hydra.py` (wandb `srnes3kk`): env=train_multiscale (SOM world), BC-init `checkpoints/bc_topsis_som/set_transformer_bc.pt` (74.1%), 600 eps, vec10, horizons U(200k,350k), eval 50, HC-v1 legacy reward, ckpts `checkpoints/hc_v3_bc`, log `reports/train_hc_v3.log`, marker `DONE_TRAIN_V3`. **Serval-side watcher** (10-min heartbeat → `reports/train_hc_v3_watch.log`, ALERT if log stale >45 min) — local watchers die when the Mac sleeps. GPU shared with ayoub's 10.7 GB job (13.7/16.4 GB total — fits).
- Caveat for records: any event-driven eval run BEFORE this fix (e.g., the 2026-07-21 local mini-benchmark of gaefix) ended episodes at the step cap, not the horizon — relative agent comparisons stand, absolute per-episode counts are cap-shaped. The 2026-07-20 benchmark generation predates the event engine and is unaffected.

## Session notes (2026-07-21 pm, part 3 — speed batch, SOM encoder, v3 launch)

- **Event-driven breakdown engine** (user items 1–5): inverse-CDF envelope sampling + state thinning (Ogata/Lewis; exact for the state-modulated hazards), unified simple/Weibull × simple/complex machines, Kijima-aware, freeze-while-broken; polling retained behind `machine.EVENT_DRIVEN_DEFAULT`. Validated: process-level distributions <5% over 3k samples; system-level 10-ep ratios straddle 1.0 (0.80/1.14, z=1.8 → variance not bias). **×5–6 wall-clock**. Plus: variable episode horizons (`max_sim_time_min/max`, uniform per reset), `pad_action_space_to_max_techs` (Discrete(max_techs) in set mode — REQUIRED for variable-fleet vec training; first v3 attempt crashed on AsyncVectorEnv space check without it), eval trims (interval 50), episodes 1200.
- **SOM ticket encoder ADOPTED over MCA** (user decision after prototype): fair features only (component identity + technology ordinal + machine process family + log base repair time + Weibull shape — all CMMS-observable; privileged f*/α* actually *worsened* the map, confirming collinearity). **Border effect found by user's challenge** (61–84% border occupancy on 10×10 = classic overparameterization) → fixed by 8×8-interior fit: adj 98±1%, sep 2.26, border 0%, diffusion mass 0.67→0.84. Canonical artifact `run_configs/embeddings/ticket_grid_som.json` (seed 2: adj 100%, sep 2.81, 0 collisions; fitted on the 31 enumerated keys, rate-weighted — NOT ticket samples, deployable-by-catalog argument). `PrecomputedEncoder` (LookupEncoder-style + hash fallback projected to the BORDER RING for unseen keys — reserved rim = feature). MCA warmup deleted from all runs (was the ~560 test warnings + a big chunk of BC slowness); MCA stays as ablation option. Comparison artifact published (hash 21%/0.99/7-coll vs MCA 67%/1.63/1 vs SOM-fair 88→98%/2.2+/0).
- **BC generations today**: hash 90.0% → MCA 91.1% → **SOM-world multiscale 74.1%** (84,277 decisions in 156 s post-speedups; 26-way variable-fleet task so lower agreement expected; histogram spans all 26 slots). Final: `checkpoints/bc_topsis_som/set_transformer_bc.pt` (local + serval).
- **gaefix training CUT at 80%** (user decision; artifacts kept: best.pt ep~300 + rounds ≤01250 — quality collapse 0.21–0.26 vs HC-v1 0.64 at industrial/30k in BOTH ckpts → §7.4 diagnostic that the collapse survives de-confounding; candidates {vec, PopArt, γλ, 500k, rotation}).
- **hc_v3 LAUNCHED 2026-07-21 15:39 local** (serval pid 3106185, wandb `zost19oo`, log `reports/train_hc_v3.log`, marker `DONE_TRAIN_V3`): env=train_multiscale (SOM world), BC-init 74.1%, 1200 eps, vec8, γ0.997/λ0.98, PopArt/no-GRU, HC-v1 legacy reward, horizons U(200k,500k), eval 5-ep/50-round. ETA ~3–5 h.
- Hydra dry-run/multirun validated; suite **474 pass** local (warnings 568→4 = MCA warmup gone), serval targeted sets green.

## Session notes (2026-07-21 pm, part 2 — R4/R5 implementation, MCA flip, seeding correction)

- **R4 implemented**: `run_configs/benchmark_suite/train_multiscale.json` — ratio-mode fleet (`techs_per_machine` 0.15–0.30, clamped so 0.30×100 = gym `max_techs` 30), machines 4–100, ALL 12 machine + 7 technician templates, MCA on. Sampled span verified: 4–26 techs / 16–96 machines. Next multiscale training: `train_hc_improved.py --env-config run_configs/benchmark_suite/train_multiscale.json`.
- **R5 implemented**: (a) **Kijima type-1 imperfect repair** — `restoration_alpha` on both breakdown processes (+`ComponentConfig` field, plumbed through `scenario.py` + `complex_machine_factory.py`); default 0.0 = perfect = historical behaviour; phase 2 (skill/fatigue-modulated α = the fatigue→quality channel) still open. (b) **Knowledge decay** — `knowledge_decay_enabled`/`knowledge_decay_interval` on `GymEnvConfig` + `KataEnv._maybe_decay_knowledge()` hook at step() top (one `decay_knowledge()` per tech per elapsed interval); default off. 4 new tests (2 Kijima + 2 decay).
- **Time anchor**: 1 t.u. ≈ 1 minute — green passage in §7.1 Scenarios (+ small-scale = deliberate stress regime) + README "Time units" table.
- **MCA flipped ON** in all 8 benchmark_suite configs + train_multiscale (was false everywhere). Consequences: next benchmark generation is MCA-world (env dynamics change for ALL agents); hash-trained ckpts (HC-v1, PO, gaefix) carry a train/eval-world mismatch there — v3 (MCA-trained) becomes the natural paper agent. **BC re-collected under MCA locally** → `checkpoints/bc_topsis_mca/set_transformer_bc.pt` (to scp to serval for the hc_v3 launch).
- **Eval seeding fix** (the REAL mechanism, see corrected bullet above): stdlib `random.seed(seed)` in eval `run_episode`.
- **HC-v1 reward restored as default for next runs** (user decision after the local mini-benchmark showed gaefix quality collapse 0.21–0.26 vs HC-v1 0.64 at industrial/30k, replicating across early best + round1200 ckpts — i.e., the collapse SURVIVES de-confounding, so the killer is in {vec, PopArt, γ0.997/λ, 500k eps, rotation-5}, not GRU/crit/GAE): potential-based knowledge shaping flipped to OPT-IN (`--potential-knowledge-reward` in train_hc_improved, `potential_knowledge_reward: false` in conf/train.yaml). Env default was already legacy; benchmark reward untouched.
- **Hydra implemented** (composition front-end only; pydantic stays the validator, `src/` untouched): `conf/train.yaml` + `conf/env/*.yaml`, `conf/agent/*.yaml` = **symlinks to the canonical JSONs** (zero duplication; JSON is valid YAML); launcher `scripts/train_hydra.py` (mirrors train_hc_improved mutations: set repr, PBRS default, popart/gru toggles, total_updates heuristic, `init_checkpoint`); `hydra.job.chdir=false` (relative paths). Sweeps: `--multirun use_popart=true,false seed=42,43,44`; per-run output dir with resolved-config snapshot, default checkpoint_dir INSIDE the run dir (`${hydra:runtime.output_dir}/checkpoints`). `dry_run=true` composes+validates without training (multirun dry-run = config-grid check). `tests/test_hydra_configs.py` (3 tests: every env group validates via KATAConfig, deep overrides land, trainer defaults). `outputs/`+`multirun/` gitignored. Suite 466.
- **§2.3 RL sub-taxonomy DONE (green)**: `sec:related:rl` + `tab:rl_taxonomy` inserted before Current-state-of-affairs; 20 works in 3 blocks (A workforce-RL 7 / B maintenance-DRL 6 / C size-generalising dispatchers 7) × 22 mark columns (Learn ON/OFF, Obj SNG/MLT, net TAB/MLP/RNN/GNN/TRF/SET, algo VAL/PG/ADP/MA, generalisation ZSW/ZSM/PRM, obs ENT/GLB, ctx MNT/PRD/WFA), Table-1 styling, totals 132 (programmatically generated+checked), This-work row = 10 marks (only MLT+SET+ZSW+ZSM+PRM combo). All 22 refs verified by subagent (21 exact, 1 author-form corrected: Solozabal Ochoa de Retana; preprint-typed: koduri/bello/schedulenet/tian). 19 new bib entries (valet was already in bib — duplicate removed); ogunfowora survey + vinyals cited in prose only. §2 roadmap sentence updated (green). Build 0 err / 32pp (needed `latexmk -gg` after the bib dedup). BONUS: `zhangReinforcementLearningMethod2022` (RCIM 73, 102227) is the verified correct reference for the old `zhangGraphNeuralNetworks2022` Table-1 mismatch blocker — swap pending.

## Review-response notes (2026-07-21 pm — user triage of the two-reviewer report)

- **R4 (scale transfer)**: sampler supports fixed/range/ratio tech-count modes, but ALL training configs use FIXED 4 techs + 12–22 machines from 6 templates → industrial eval (30 techs, 80–100 machines, 12 templates) is doubly OOD. Fix = ratio/range training config (roadmap curriculum item). Not yet changed.
- **R5 asides/plans**: (a) time anchor proposal 1 t.u. ≈ 1 minute (travel 15 min, MTTR ~1.5 h, very-long ≈ 9.5 y career-scale; caveat: no shifts — frame as continuous-coverage simplification); (b) small-scale availability 0.33 → justify as DELIBERATE STRESS regime (write into §7/limitations later); (c) **imperfect repair**: `repair_kijima_type1` exists in funcs.py, ZERO call sites — wiring plan: call in component.repair() with per-component `restoration_alpha` (default perfect), phase 2 = modulate alpha by tech skill/fatigue → becomes the missing fatigue→QUALITY channel; (d) **skill decay**: `GymTechnician.decay_knowledge()` exists (delegates to ongoing grid), no caller/no config — wire a rate + trigger; decay-enabled sensitivity run directly counters the "saturation is an artifact" critique; (e) fatigue→illness ALREADY implemented (exhaustion DisruptionTypeConfig, polls fatigue) and documented in appendix — no paper change needed.
- **R6 (encoder)**: `use_mca_encoder: false` in ALL benchmark configs → every experiment (incl. gaefix retrain) uses the HASH encoder; user expected MCA-by-default. Decision pending: recommend keep hash for the current generation + footnote TRF in Table 1, run MCA as explicit ablation (flipping it changes env dynamics for all agents → whole-benchmark switch, and BC data would need recollection).
- **R1 (selection leakage)**: NOT factual — training/model-selection lineage seeds 42/1235 vs benchmark 4321, disjoint by construction; green sentence added to §7.1 Protocol stating it + committing final benchmarks to freshly drawn seeds; `--eval-seed` CLI added to eval script.
- **R3 (decision-frequency incentive)**: assessed as second-order here — in v2 industrial data n_steps ranks almost exactly with finished products (healthy line → more tickets), so the incentive currently aligns WITH production; do NOT go terminal-only (destroys dense credit). Plan: limitation ¶ + cheap decision-count-vs-return diagnostic + optional γ^Δt semi-MDP flag later.
- **M3 DONE (green)**: §5.2 restructured → 5.2 The Agent {5.2.1 Agent Logic, 5.2.2 Agent Architecture: HTT-RL (new — hybrid fusion PLE/T2V/Fourier w/ cites, slot pooling, cross-slot set transformers, cross-attn refiner, pointer head, PPO hyperparams)}; §5 intro + §7.1 Agents ¶ aligned; `schulman2017proximal` + encoder cites verified in bib. Green = \textcolor{green!55!black} (user's revision-marker convention alongside red).
- Statistical/claims fixes deferred until best agent's numbers land (user decision).

## Session notes (2026-07-21 — Tier-1 improvements batch)

- **MLM pretraining dropped** (user decision): 7 live manuscript mentions scrubbed (see Key decisions); build 0 err / 29pp.
- **Small-fixes bundle**: inline-eval stream-state guard (`snapshot_stream_state`/`restore_stream_state` + try/finally in `_inline_eval`); PopArt→AdamW moment rescale (exp_avg ×c, exp_avg_sq ×c²); λ 0.97→0.98 in `set_transformer_gru.json`; ~~numba RNG bug~~ **CORRECTED 2026-07-21 pm**: `step_degrade` is DEAD legacy code (the whole `funcs.py` numba kernel has zero callers) — the LIVE failure draw is stdlib `random.uniform(0,1)` in `Machine._breakdown_driver` (machine.py:124). Vec training was correctly seeded all along (`SeededResetWrapper` seeds stdlib `random` too); the real gap was the EVAL harness seeding only `np.random` → benchmark failure sequences uncontrolled across agents (same practical conclusion as review-M2, different mechanism). Fixed: `random.seed(seed)` added to `run_episode`; the `seed_numba_rng` shim stays as harmless future-proofing. NOTE: commit `2613a9f`'s message describes the wrong mechanism.
- **Potential-based knowledge reward**: `knowledge_increment_potential_based` (+`knowledge_potential_gamma`) = F = γₚ·Φ(s′)−Φ(s), un-floored, policy-invariant; default OFF (benchmark compat), ON by default in `train_hc_improved.py` (`--legacy-knowledge-reward` opt-out).
- **TOPSIS BC warm-start** (`scripts/warmstart_bc.py`): rotating-layout collection → masked-CE clone → trainer-compatible ckpt (vocab embedded); launcher `--init-checkpoint`. Teacher = TOPSIS (not greedy_reward) for fairness — no reward-oracle info even at collection. Smoke: 68.7% agreement on tiny run.
- Launcher eval episodes 2→5. Env got `technician_fatigues()`; TOPSIS falls back to it when obs lacks the structured fatigue field (set-mode support). Suite **459 pass**.
- **Queue when `hc_v2_gaefix` lands** (watcher active): (1) verify rc, (2) sync today's code to serval, (3) benchmark gaefix at industrial vs greedy 2217/TOPSIS 2202, (4) launch `hc_v3_bc` = BC-init + PBRS + λ0.98 training (`--init-checkpoint checkpoints/bc_topsis/set_transformer_bc.pt --checkpoint-dir checkpoints/hc_v3_bc`). NOTE: running gaefix predates all of today's changes (λ0.97, legacy knowledge reward) — it stays the clean GAE-only ablation point.

## Session notes (2026-07-20, part 2 — review, GAE fix, retrain)

- **Full audit of prior (Opus-session) work** at user request: benchmark numbers re-pulled from serval CSVs (exact), Table 1 recounted cell-by-cell incl. parent-diff invariant (only the 9 dropped columns changed; 405 ✓), §7's 8 tables byte-identical to generated artifacts + ~40 inline numbers recomputed. Real defects found: the GAE bug, 3 false §7 sentences, the `_inline_eval` RNN clobber, HC-v2 reward confound (all in Blockers). Reward-probe/baseline/PopArt/GRU-eval/vec-seeding code verified correct.
- **4 more baselines** (same session, committed): `EmpiricalSPTAgent`/`EmpiricalTopsisAgent` — *honest-information* twins learning per-(tech, failure-key) means from the new env `repair_log()` (dispatcher now passes `tech` to `_on_repair_completed`, 3-arg backward-compat; `failure_key()` = `"mtype:component_type"`); `BatchMILPAgent` — batch assignment under balance cap ceil(K/n_avail), solved exactly via Hungarian on capacity-replicated columns (transportation reduction), 4×n_techs window; `ReserveSpecialistAgent` — weakest tech with ETA ≤ τ·best (τ=1.5; τ=1 ≡ SPT). Eval keys `empirical_spt, empirical_topsis, batch_milp, reserve_specialist`. `analyze_hvp_results.py` labels carry † (informed) / ‡ (reward oracle), unmarked = honest; per-scenario `table_footnote.tex` emitted. `tests/test_baselines.py` → 15; suite 454. Real-env smoke: 228 repairs logged/episode, estimator learned 46 pairs.
- **§2 polish** (paper submodule): title "+, and Methods" (Method block is inside the Common-parameters span), mapping-¶ reordered (adoption → findings → conventions → extensions → post-hoc note), §2.2 opener rewritten, 6 cluster-1 citations added, `~\cite` fix, stray tabular blank line removed. Build 0 err / 29pp. Red `\textcolor` markers left for author sign-off.
- **Ops**: serval is shared (ayoub's CPU training runs there — never broad-pkill python); `pkill -f`/`pgrep -f` patterns self-match the ssh remote shell and kill the session — use `[x]` bracket trick; a WireGuard drop presents as ssh timeout to 10.6.36.22.

## Session notes (2026-07-20)

- **HC-v2 benchmark + 5 new baselines** (serval-paris = paris-snt-unit, RTX 4080, ssh alias; out-root `reports/hvp_eval_v2/`, NOT v1 `reports/hvp_eval/`). Real HC-v2 ckpt was on serval `checkpoints/hc_v2/set_transformer_best.pt` (GRU+PopArt); local `set_transformer_best.pt` scp was a stale byte-dup of the PO ckpt. `scripts/eval_human_vs_performance.py` now **architecture-aware** (reads each ckpt's `improvements` dict → builds matching GRU/PopArt net). Industrial 100k (25k-cap re-run — default 10k cap TRUNCATES) finished: greedy_reward 2217 > topsis 2202 > **HC-v1 2082** > Hungarian 2069 > SPT 2049 > shortest_queue 2025 > … > hc_v2 1894 > PO 1863 > … > train_weakest 1614. See memory `benchmark-results-hvp`.
- **5 new baselines** (in `src/agents/baselines/heuristics.py`; parent commits 350b45c + bcefecb, now on `main` as ancestors of paper push fccb321): `ShortestProcessingTimeAgent` (SPT/skill-greedy), `OptimalAssignmentAgent` (Hungarian, scipy `linear_sum_assignment`), `TopsisAgent` (multi-criteria, Ferjani2017), `GreedyRewardAgent` (myopic argmax of HC reward), `TrainWeakestAgent` (naive upskill). Run: `--agents shortest_processing,optimal_assignment,topsis,greedy_reward,train_weakest --merge`.
- **Env decision-support API** (`src/kata/env.py`, public, current-ticket-relative): `expected_repair_times()`, `skill_match_scores()`, `available_mask()`, `assignment_counts()`, `assignment_cost_matrix()`, side-effect-free `assignment_reward_estimates()` (snapshots/restores `_prev_finished_products`/`_prev_fleet_knowledge`/normaliser freeze). New structured-obs field `technician_expected_repair` (flag `include_repair_estimate_in_observation`, default on); `Agent.attach_env()` gives baselines the handle (eval calls it). `tests/test_baselines.py` (10 tests incl. greedy-trap + reward-probe idempotency); full suite **444 pass**.
- **§2 + Table 1 restructure** (paper 113ac5f → parent fccb321): see Key decisions row. Build 0 err / 29pp.

## Session notes (2026-07-07, part 2)

- Manuscript builds **0 errors / 0 warnings** (28pp). §4 reworked (agent-vs-dispatcher fix, design-properties paragraph, rich figure caption); Appendix A: six-entity fix, φ hash-encoder explanation, ρ semantics + archetype-axis rationale + specialists documented, disruption trigger taxonomy + fatigue→exhaustion loop, defaults (f=0.3, α=0.15, Δ_travel=15), reward-stack paragraph replaced by pointer to §5.4, sampler paragraph updated to 3-mode + episodes_per_scenario, Bernoulli per-cycle clarification, eval-protocol cross-ref. §6.1 now states unit coefficients + normalization.

## Session notes (2026-07-06)

- Code side (uncommitted): signed RepairTimeDelta, `mttr_rolling`/`mtbf`/`workload_balance` metrics, 3-mode technician-count scenario sampler + `episodes_per_scenario`, knowledge-sensitivity retune 0.002→0.15 (old default was a bug), new tests.
- Paper submodule work also uncommitted: Grillo realignment, §5, bib repairs (`grillo2022hrap40`→`grilloHumanResourceAllocation2022`, added `kuhn1955hungarian`/`bouajaja2017survey`, tien `collaborator`→`author`), `bm` package, `\graphicspath`, duplicate-label fixes.
